{
  "name": "@technoqueue/runner",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx tests/local-execution.test.ts"
  },
  "dependencies": {
    "@technoqueue/core": "workspace:*",
    "commander": "^14.0.0",
    "tsx": "^4.20.0",
    "zod": "^4.0.0"
  }
}
