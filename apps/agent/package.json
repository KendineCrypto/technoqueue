{
  "name": "@technoqueue/agent",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.8.0",
    "@technoqueue/core": "workspace:*",
    "commander": "^14.0.0",
    "tsx": "^4.20.0"
  }
}
